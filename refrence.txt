
 export LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8
 cd ios && pod install

fvm flutter clean
fvm flutter pub get
 cd ios
 rm -rf Pods Podfile.lock
 pod install --repo-update
 cd ..


# 2. 清理 Flutter 构建
fvm flutter clean
# 3. 清理 iOS 相关缓存（很重要）
rm -rf ios/Pods ios/Podfile.lock
rm -rf ~/Library/Developer/Xcode/DerivedData/*
# 4. 重新装依赖并构建
cd ios && pod install && cd ..
fvm flutter build ios   # 或从 Xcode Archive

一台设备
fvm flutter run --profile -d ios
多台设备
fvm flutter run --profile -d 00008110-0002150921E2401E
fvm flutter attach -d 00008110-0002150921E2401E
fvm flutter attach -d 00008110-0002150921E2401E --app-id com.wallet.primary.pay
fvm flutter devices

iOS真机签名失败
fvm flutter clean
rm -rf ios/Pods ios/Podfile.lock ios/.symlinks ios/Flutter/Flutter.podspec
rm -rf ~/Library/Developer/Xcode/DerivedData
fvm flutter pub get
cd ios && pod install --repo-update && cd ..
